{
  "name": "tokengate",
  "version": "0.1.0",
  "type": "module",
  "description": "Inline semantic guardrail cho LLM streaming: chặn token vi phạm trước khi nó chạm màn hình.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --env-file-if-exists=.env test.js && node --env-file-if-exists=.env test-config.js && node --env-file-if-exists=.env test-proxy.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Thanh-Mathieu95/jev-model-tokengate.git"
  }
}
